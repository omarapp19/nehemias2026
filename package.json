{
  "name": "nehemias",
  "version": "0.1.0",
  "private": true,
  "description": "Proyecto Nehemías — Plataforma de Gestión Humanitaria",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "db:generate": "pnpm --filter @nehemias/db generate",
    "db:migrate": "dotenv -e .env -- pnpm --filter @nehemias/db migrate",
    "db:deploy": "dotenv -e .env -- pnpm --filter @nehemias/db deploy",
    "db:seed": "dotenv -e .env -- pnpm --filter @nehemias/db seed",
    "db:studio": "dotenv -e .env -- pnpm --filter @nehemias/db studio"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.4",
    "prettier": "^3.3.3",
    "turbo": "^2.3.3",
    "typescript": "^5.6.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "argon2",
      "esbuild",
      "sharp"
    ]
  }
}
